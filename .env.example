# Base de datos PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USER=morse
DB_PASSWORD=morse_secret_123
DB_NAME=morse_flashcards

# JWT
JWT_SECRET=change-this-to-a-random-secret

# Puerto del servidor (por defecto 3000 con adapter-node)
PORT=3000
